# Slide del talk

Le slide finali del talk verranno aggiunte qui dopo il WordCamp Torino 2026 (PDF esportato da Google Slides).

Nel frattempo, in questa repo trovi tutto il resto del materiale tecnico per replicare la demo:

- `../animations/` — le tre animazioni HTML mostrate dal palco
- `../workflows/` — i workflow n8n di ingestione e query
- `../contenuti/` — i 20 contenuti del sito demo in markdown
- `../docker/` — Docker Compose per Qdrant + n8n
- `../chat.html` — interfaccia chat che chiama il webhook n8n
